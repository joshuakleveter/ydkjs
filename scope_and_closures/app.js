/**
 * LHS (left hand side) assignment.
 *
 * In LHS we don't care about the current value,
 * we just need the container so we can assign it a value.
 */
let a = 'value';

/**
 * RHS (right hand side) assignment.
 *
 * In RHS we need to get the value from the container.
 */
console.log(a);
